class Person {
  // Recebo o nome e a idade
  constructor (name, age) {
    // E adiciono nas propriedades da pessoa
    this.name = name;
    this.age = age;
  }
}

// Praticando o que foi visto na aula //
class Car {
  constructor (brand, model) {
    this.brand = brand;
    this.model = model;
  }
}